[package]
name = "cim-core"
description = "CIM (Block Range Packing) lossless image codec"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
# Criterion owns benchmarking; keep the default libtest harness out of `cargo bench`.
bench = false

[dependencies]
thiserror = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
criterion = { workspace = true }

[[bench]]
name = "throughput"
harness = false
